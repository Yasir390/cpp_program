#include<iostream>
using namespace std;

int main(){
    int x,y;
    float z;

     cout<< x+y;

     // post increment all are same thing
     x++;
     x = x+1;
     x +=1;
    
    // pre increment
     ++x;

     // post decrement all are same thing
     x--;
     x = x-1;
     x -=1;


     x++ ;// it means at first use value of x , then increment
     ++x ;// it means at first increment the value of x , the use 
}